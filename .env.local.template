# ---------------------------------------------------------
#  Trading Journal OS — Local Environment
#  Fill in your values from the services below.
#  This file is gitignored. DO NOT commit it.
# ---------------------------------------------------------

# ── Supabase ─────────────────────────────────────────────
# Paste your Project URL from: Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co

# Paste your anon/public key from: Supabase Dashboard → Settings → API → anon (public) key
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# Paste your secret key from: Supabase Dashboard → Settings → API → secret key
# (This is the service_role key — it bypasses RLS. Keep it secret.)
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Paste your connection string from: Supabase Dashboard → Settings → Database → Connection string (URI)
# Format: postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
DATABASE_URL=postgresql://postgres:PASSWORD@db.xxxxxxxxxxxx.supabase.co:5432/postgres

# ── Inngest ──────────────────────────────────────────────
# Paste your Event Key from: app.inngest.com → Your App → Keys
INNGEST_EVENT_KEY=your-event-key-here

# Paste your Signing Key from: app.inngest.com → Your App → Keys
INNGEST_SIGNING_KEY=your-signing-key-here

# ── OpenAI (leave commented until AI Coach phase) ────────
# OPENAI_API_KEY=sk-...

# ── App ──────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
