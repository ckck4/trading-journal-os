import { PlaceholderPage } from '@/components/ui/placeholder-page'
import { Layers } from 'lucide-react'

export const metadata = { title: 'Settings — Strategies' }

export default function SettingsStrategiesPage() {
  return (
    <PlaceholderPage
      title="Strategy Settings"
      description="Manage your strategy library — names, descriptions, and active status."
      icon={Layers}
    />
  )
}
