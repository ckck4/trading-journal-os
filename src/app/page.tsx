// The root `/` route is handled by src/app/(app)/page.tsx via the (app) route group.
// This file must exist (created by create-next-app) but has no default export
// so Next.js does not register it as a page.
export {}
